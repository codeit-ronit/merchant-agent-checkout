"""The commerce eval suite — SEPARATE from SENTINEL's golden set, with its own
count (ADR-029). Proves three things: the loop works, the controls hold, and
the controls do not block legitimate business (08-EVAL §2)."""
