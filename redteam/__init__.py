"""The red-team harness — this produces the evidence. Everything else is the
argument; this is the demonstration.

Runs EXCLUSIVELY against the local fixture server (enforced + tested). Grading is
rule-based and deterministic, derived from the trace and audit log — never a
model judging outputs.
"""
