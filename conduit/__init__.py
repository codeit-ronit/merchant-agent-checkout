"""CONDUIT — the agentic commerce loop, built on SENTINEL.

Catalog, cart, and mandate are MODELLED services over real test-mode
primitives; every Razorpay call is real. SENTINEL is a dependency: the
enforcement boundary every tool call crosses. See docs/spec/buildathon/.
"""
